spring:
  application:
    name: chokchok-front

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true

# Eureka Client ??
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:1234@localhost:8761/eureka/  # Eureka ?? URL (??? ?? ??)